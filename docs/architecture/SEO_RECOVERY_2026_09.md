# SEO 회복 전략 — 2026-07 노출 절벽 재감사 (2026-09-11)

메모리·주석·이전 실측을 전부 무시하고 GSC·Cloudflare·라이브 프로브·코드를 처음부터 다시 봤다.
이 문서는 **무엇이 사실로 확인됐는지**, **무엇이 원인이 아닌지**, 그리고 **회복을 위해 무엇을
어떤 순서로 하는지**를 기록한다. 숫자는 전부 2026-09-11 GSC(`sc-domain:siglens.io`) 실측이다.

## 1. 증상 — 하루 만에 15배 절벽, 이후 무회복

| 구간 | 클릭/일 | 노출/일 | 비고 |
|---|---|---|---|
| 2026-04 ~ 05 중순 | 0~2 | 0~30 | 사이트 초기 |
| 05-24 ~ 06-30 | 10~15 | 800~1,270 | 유일한 "잘 되던" 5주 |
| **07-01 → 07-02** | 3 → 3 | **471 → 31** | 하루 만에 15배 |
| 07-02 ~ 09-10 | 0~3 | 구간 평균 11~17 | 무회복 (3개월 클릭 305). 평균 07-03~25 16.9, 07-26~09-08 11.0 |

- 절벽 직전의 공식 Google 업데이트는 **June 2026 spam update(06-24 ~ 06-26)** 다. 2026년 core update는
  03-27·05-21 두 번뿐이고 **7월 core update는 없다**(Google Search Status Dashboard 순위 사건 기록). 절벽은
  spam update 종료 6일 뒤, 사이트 장애 주간(§2-1) 직후에 났다.

> 정정(2026-09-11): 초판은 절벽을 "7월 core update(06-28 ~ 07-09) 한가운데"로 적었으나 그런 업데이트는 없다.
> 2025년 6월 core update(2025-06-30 시작)와 섞인 오류다. 아래 §2-1·§3·§5·§7·§7-1도 같이 고쳤다.
- 같은 날(07-02) Googlebot 크롤이 25K/일로 급증했다가 07-10 이후 ~1K/일로 주저앉았다 — Google이
  사이트를 재평가하고 크롤 예산을 회수한 궤적이다.
- 6월에 클릭을 만들던 페이지는 거의 전부 `/{ticker}/fear-greed` 롱테일(`SOXL`·`SNDK`·`RKLB`·`MU`·`TTWO`·`QLD`…)과
  "`<종목명> 주가`"(하마마츠 포토닉스·PSMC·AEIS…) 검색이었다. 지금 그 검색어로 시글렌스는 1페이지에 없다.

## 2. 원인이 **아닌** 것 (전부 실측으로 배제)

| 가설 | 결과 |
|---|---|
| 수동 조치 / 보안 문제 | 없음 (GSC 두 보고서 모두 "감지된 문제 없음") |
| robots.txt / noindex 실수 | `/`, `/AAPL`, `/SOXL/fear-greed` 전부 `index, follow` + self-canonical, 색인됨, 최근 크롤 09-01 |
| 서버 오류(현재) | 90일 응답 99% 200, 5xx <1%. **단 07-02 하루는 예외** — §2-1 |
| 호스트 상태 | "최근 90일간 호스트 문제 없음" (GSC 기준; 실제 장애는 §2-1) |
| Cloudflare 차단 | Googlebot 요청 99% 성공 — 차단·챌린지가 있으면 나올 수 없는 수치. Cache Rules 2개(HTML/RSC 분리)는 정상 |
| 국제화(hreflang/canonical) | `/en/*` noindex, hreflang 미광고, `/ko/*`→`/*` 301. 문제 없음 |
| SEO 메타·라우팅 회귀 | 06-28 ~ 07-02 배포는 메타·라우팅을 건드리지 않음. 로그는 14일 보존이라 당시 응답 본문은 확인 불가 |

### 2-1. 단, 절벽 주간에 사이트 장애가 겹쳐 있었다 (2차 검토, 2026-09-11)

크롤 통계를 응답 코드별로 다시 보면 "기술은 무죄"가 아니다:

| 날짜 | 사이트 쪽 사건(`DEPLOY_RUNBOOK.md` 인시던트 기록·git) | Googlebot이 본 것(GSC 크롤 통계) |
|---|---|---|
| 06-15~24 | 정상 | 200 크롤 10~13K/일 (잘 되던 시기) |
| 06-24 ~ 06-26 | Google **June 2026 spam update** 롤아웃(2일) | — |
| **06-26** | 외부 API 402가 uncaught로 던져져 `/`·`/economy`가 **0-byte ISR 캐시로 동결**(최대 24h) | — |
| 06-27 | `/economy`·`/market` prerender에 FMP 키 누락 수정 배포 | — |
| **06-28** | **디스크풀 → 루트 FS read-only**, 인스턴스 교체로 복구. ISR 캐시 S3 외부화 배포(v0.30~31) | **06-27 ~ 07-01 크롤 ~0/일** — Googlebot이 5일간 사실상 크롤을 멈췄다 |
| **07-02** | v0.34.0 배포. FMP EOD 지연 대응 커밋("publish-lag robustness") | **크롤 25K 폭발** — 그중 **404 2.3K**(90일 중 유일한 스파이크), 5xx 110, 평균 응답 1.3s |
| 07-02 → 07-03 | — | 노출 471 → 31 |
| 07-08 | 롱테일 noindex 게이트 배포 | 크롤 ~1K/일로 안착, 이후 무회복 |

읽는 법: 사이트가 6/26~7/1 가장 불안정했던 바로 그 주에 Google이 크롤을 멈췄다가, 7/2 하루에 전 사이트를
다시 읽었다(84% refresh). 그날 2.3K URL이 404였고(정황상 데이터 공급자 장애로 `notFound()`에 떨어진 심볼
페이지), 나머지 수만 페이지는 원래부터 템플릿 한 문단이었다. **spam update 직후의 재평가와 사이트 최악의 순간이
같은 날에 만났다.** 둘 중 어느 쪽이 결정적이었는지는 분리할 수 없다 — 그러나 처방은 같다: 코퍼스 품질 + 안정성.

## 3. 원인 판정 — 사이트 단위 품질 강등 (+ 장애 주간의 재크롤)

Google이 색인해 둔 사이트의 모습(2026-06 시점):

- 색인 **50,873** URL. 그중 sitemap이 의도한 것은 **3,596**. 나머지 ~47K는 롱테일 티커 × 9탭.
- 롱테일 페이지의 본문은 "`XXX은 최근 종가 $… 기준으로 직전 봉 대비 …% 하락했습니다. RSI …`" 템플릿
  한 문단이 전부다. `site:siglens.io` 결과의 스니펫이 전부 이 문장이다 — Google이 본 이 사이트의 "콘텐츠"가 그것이다.
- YMYL(투자) 도메인에서 AI 생성 문장을 수만 페이지에 뿌리고, 운영 주체·방법론·저자 정보가 없다.

즉 Google은 — June 2026 spam update의 scaled content abuse 판정이든, 상시 도는 core 품질 시스템이든(GSC로는
둘이 구분되지 않는다) — 이 사이트를 **"숫자만 바뀌는 템플릿 페이지 수만 개짜리 프로그래매틱 금융 사이트"**로
분류했고, 6월에 잠깐 잡혔던 롱테일 순위가 통째로 회수됐다. 기술적 차단이 아니라 품질 판정이다.

## 4. 지금 상태 — 강등 이후 두 달 동안 코퍼스가 줄지 않았다

07-08에 롱테일을 `noindex`로 게이트했지만(`evaluateSymbolIndexability`), Google 쪽 색인 수는 그대로다:

| GSC 페이지 보고서 (09-04 기준) | 수 | 해석 |
|---|---|---|
| 색인 생성됨 | **50,873** | 07월 이후 거의 불변. noindex를 아직 못 본 URL이 ~47K |
| NOINDEX로 제외 | 14,743 | 두 달간 재크롤로 noindex를 확인한 양 |
| **발견됨 – 현재 색인 생성되지 않음** | **45,635** | 08-07~08-16 removal sitemap 제출 직후 0 → 45K로 급증. 전부 **한 번도 색인된 적 없는** 롱테일 |
| 크롤링됨 – 색인 생성되지 않음 | 12,324 | 롱테일 |
| 크롤 예산 | ~3.2K 요청/일, HTML은 28% | HTML 크롤 샘플 10/10이 롱테일 noindex 페이지 |

핵심: `sitemap-removal-{chart,news,overall}.xml`(총 82K URL)은 **색인된 적 없는 45K URL을 발견 큐에
밀어 넣었고**, 정작 색인돼 있는 47K의 재크롤은 앞당기지 못했다. 하루 ~900건의 HTML 크롤이 이 큐에
쓰이고 있다.

부수 관측:
- 크롤의 58%가 "다른 파일 형식"(text/x-component = RSC 페이로드), 69%가 "페이지 리소스 로드" — 렌더 한 번에
  다른 심볼 페이지의 RSC를 끌어온다. 코드상 `prefetch={false}`는 v0.52.5부터 대부분 적용돼 있어 90일 통계의
  잔상일 가능성이 크지만, 09-09~10 샘플에도 남아 있다. Cloudflare Speed Brain(speculation rules)이 켜져 있다.
- `/{ticker}/position` 402 URL이 PR #791(2026-09)로 sitemap에 추가됐다. SSR 고유 텍스트 868~1,222자
  템플릿(형제 탭은 2~8천 자), 16개월 노출 0건.

## 5. 전략 — 세 축, 우선순위 순

회복은 "Google이 이 사이트를 다시 평가할 때 색인 코퍼스가 대부분 유용한 페이지여야 한다"로 귀결된다.
강등은 특정 업데이트 날짜를 기다려 풀리는 게 아니다. Google 문서 기준 core 계열은 큰 core update를 꼭 기다릴
필요 없이 발표되지 않는 작은 core update로도 풀릴 수 있고 사이트 전체 재판정에 수 개월이 걸린다. spam 계열은
정책 준수를 시스템이 수 개월에 걸쳐 학습한 뒤 풀린다. 어느 쪽이든 **주 단위가 아니라 월 단위**다.

### A. 색인 코퍼스를 3,200개 안팎의 "산문이 있는 페이지"로 좁힌다 (최우선)

| # | 조치 | 어디서 | 상태 |
|---|---|---|---|
| A1 | `sitemap-removal-{chart,news,overall}.xml` GSC 제출 **삭제** | GSC › Sitemaps | 대시보드 (사용자 확인 후) |
| A2 | `/{ticker}/position` 항상 noindex + sitemap 제외 (#791 되돌림) | 코드 | **이 PR** |
| A3 | 인기 종목이라도 스냅샷 산문이 없는 탭은 noindex — 이미 `degraded` 게이트가 담당 | 코드 | 유지 |
| A4 | 롱테일 noindex 유지. 6월 트래픽이 거기서 났더라도 되돌리지 않는다 — 그게 강등 사유다 | 코드 | 유지 |

A1의 근거: removal sitemap의 목적(noindex 재크롤 유도)은 두 달간 색인 수를 1건도 줄이지 못했고, 대신 45K의
미색인 URL 발견을 만들었다. 삭제하면 HTML 크롤 예산이 사이트맵·내부 링크로 도달하는 URL, 즉 색인돼 있는 것들의
refresh 크롤로 돌아간다. 라우트는 남겨 둔다(재제출이 필요하면 좁혀서).

### B. 남는 페이지의 신뢰 신호 (YMYL)

| # | 조치 | 상태 |
|---|---|---|
| B1 | `/about`(운영 주체·분석 방법론·데이터 출처·갱신 주기·면책·문의) 페이지 + 푸터 링크 + `Organization.founder` 연결 | **이 PR** — 핸들 `y0ngha`·이메일·GitHub 공개(사용자 결정 2026-09-11 — 실명 대신 핸들). 설계: `docs/superpowers/specs/2026-09-11-about-page-design.md` |
| B2 | 종목 페이지 `WebPage` JSON-LD에 `dateModified`·`publisher` | 후속 PR (B1과 묶음) |
| B3 | `/{ticker}/fear-greed`는 6월에 실제로 검색을 받던 유일한 탭인데 스냅샷 산문이 없다(2.5K자, 숫자만). 스냅샷 탭 추가는 core+cron 작업 | **보류**(2026-09-11) — 7탭 서술 도입(07-25) 전후 사이트 노출이 16.9 → 11.0회/일로 이득 신호가 없고, 비브랜드 클릭은 서술 없는 FG 탭이 대부분. 비용 월 $25~45 + 개발 3~4일. 배포 후 3개월에 서술 탭 vs FG 탭 노출로 재판단 |

### C. 크롤 효율 (보조)

| # | 조치 | 상태 |
|---|---|---|
| C1 | Cloudflare Speed Brain 끄기 — 렌더마다 `/cdn-cgi/speculation` fetch, 이득은 pointerdown prefetch뿐 | 대시보드 (사용자 확인 후) |
| C2 | `SectorFactsSummary` 등 `prefetch` 미지정 Link 잔여 4곳 | 낮음 — 별건 |

## 6. 이 PR이 하는 것 / 하지 않는 것

**한다**: A2. `[symbol]/position` generateMetadata를 항상 `noindex, follow`로, `buildPopularEntries`·
`buildCryptoPopularEntries`에서 position 엔트리 제거(−402 URL, sitemap-popular 3,433 → 3,031, crypto 145 → 116).
공유 카드용 훅 카피(title/OG/Twitter)는 유지.

**한다 (2차)**: B1. `/about` 페이지(운영 주체 `y0ngha`·방법론·출처·갱신·한계·면책·문의), 푸터 링크, 정적 sitemap, `AboutPage`·`Person` JSON-LD와 홈 `Organization.founder` 연결.

**하지 않는다**: 롱테일 재개방, removal 라우트 삭제, fear-greed 스냅샷.

## 7. 측정 — 무엇이 움직여야 "먹혔다"인가

| 지표 | 지금 | 기대 | 어디서 |
|---|---|---|---|
| 색인 생성됨 | 50,873 | 4~8주 안에 **감소 시작** (A1 뒤 refresh 크롤이 noindex를 보기 시작) | GSC › 페이지 |
| 발견됨-미색인 | 45,635 | 정체 또는 감소 (신규 발견 0) | GSC › 페이지 |
| HTML 크롤 샘플 | 10/10 롱테일 | 인기 종목 비율 상승 | GSC › 크롤링 통계 › HTML |
| 노출/일 | 11.0 (07-26~09-08 평균) | 4~8주 안에는 **변화 없어도 정상**. 배포 후 3~6개월 안에 올라야 한다 | GSC › 실적 |

노출이 오르지 않는 것 자체는 실패 신호가 아니다. 색인 수가 두 달 뒤에도 50K대면 A1이 안 먹힌 것이고,
그때는 removal sitemap을 "실제로 색인된 URL"로 좁혀 재제출한다(GSC 실적 16개월 페이지 목록 + `site:` 샘플).

## 7-1. 이 사이트 특성(금융·투자·AI 생성)이 받는 제약 — 2차 검토

| 제약 | 무엇인가 | 시글렌스에 뜻하는 것 |
|---|---|---|
| Google YMYL | 금융은 E-E-A-T 가중치가 가장 높은 영역. 평가 기준은 "누가·어떻게·왜 만들었나", 사람 검토, 출처·갱신일·면책 | `/about`(운영 주체·방법론·출처), 페이지마다 "기준 시각"·면책, "전망"·"매매 신호"류 **성과 암시 문구 최소화** |
| Google 스팸 정책 — scaled content abuse | 생성 수단과 무관하게 "검색용으로 대량 생성된 저가치 페이지" 자체가 정책 위반. 절벽이 June 2026 spam update(06-24~26) 직후에 났고 이 사이트 프로필과 맞는다 | 롱테일 재개방은 정책 위반 복귀. 인기 종목이라도 산문 없는 탭은 색인 금지 |
| 주가·티커 쿼리의 SERP 구조 | Google은 시세·티커 질의에 AI Overview를 거의 쓰지 않고 전통 랭킹을 유지 → Investing.com·TradingView·네이버·알파스퀘어가 독식 | "`<종목> 주가 전망`"에서 신생 도메인이 이기는 건 구조적으로 어렵다. 차별화 가능한 건 "이 사이트에만 있는 서술"(종합 결론·공포탐욕 해석)뿐 |
| 국내 규제 — 유사투자자문업(자본시장법 §101) | 불특정 다수에게 **대가를 받고** 금융투자상품 투자판단·가치 조언을 통신물로 제공하면 금융위 신고 대상. 2024 개정으로 유료 리딩 규제 강화 | 현재 코드에 결제 경로 없음(tier는 free/member/pro, BYOK) → 대가 요건 미충족일 가능성. **유료화하는 순간 신고 대상.** "매매 신호"·차트 Buy/Sell 마커·"전망" 표기는 그때 재검토. 법률 자문이 아니므로 유료화 전 금감원/변호사 확인 |

## 8. 하지 말아야 할 것

- 롱테일 `noindex` 해제. 6월의 클릭은 강등 직전의 일시적 상태였다.
- 82K URL짜리 removal sitemap 재제출. 발견 큐만 키운다.
- 순위가 안 오른다고 인기 종목 목록을 늘리는 것 — `SITEMAP_SCOPE.md` §2 순서(prewarm → 용량 → 깊이 실측 → sitemap)가 먼저다.

## 9. 2026-09-17 운영 렌더 감사 — 크롤·렌더 실측

v0.79.1 배포 직후 운영을 **밖에서** 다시 봤다. 코드가 아니라 Google이 받는 응답이 기준이다.

- 원시 HTML: sitemap 3,166 URL + 비색인 샘플 22개 전수, Googlebot 스마트폰 UA, `Accept-Encoding: gzip, br`.
- JS 렌더: Playwright(Chromium)로 31개 페이지를 Googlebot 스마트폰 UA와 사람 UA로 각각 렌더해 DOM·콘솔·네트워크를 비교.
- 종목 간 고유도: 탭별로 본문을 숫자·종목명 정규화한 뒤 12자 shingle을 만들고, 같은 탭 페이지의 30% 이상에 나오는 shingle을 "공통"으로 셌다.
- Google 공식 기록: Search Status Dashboard 순위 사건, 스팸 정책 문서(2026-08-28 개정).

### 9-1. Google 쪽 — 기다릴 업데이트가 없다

| 사건 | 기간 |
|---|---|
| March 2026 spam / core | 03-24~25 / 03-27~04-08 |
| May 2026 core | 05-21~06-02 |
| June 2026 spam | 06-24~06-26 (절벽 직전) |
| August 2026 spam | 08-18~08-20 |

09-17 기준 진행 중인 롤아웃은 없고, 5월 이후 core update도 없다. 사이트 단위 재평가는 다음 core update가
계기가 될 가능성이 크다. 그때 색인 코퍼스가 "산문이 있는 페이지"만 남아 있어야 한다.

### 9-2. 깨끗했던 것 (수정 불필요)

| 항목 | 실측 |
|---|---|
| 상태 코드 | 3,185 중 200이 3,180. 404 3건(가짜 티커·만료 공유·없는 카테고리)과 307 1건(`/portfolio`→로그인)은 의도 |
| 리다이렉트 | http→https·www→apex 301, 끝 슬래시 308, 소문자 티커 301, `/ko/*` 301 |
| canonical | 색인 대상 2,918개 전부 자기 참조 1개. `?utm_*`·`?tab=`은 깨끗한 URL로 정규화. `og:url` = canonical |
| robots | 메타와 JS 렌더 후 값이 같다(렌더가 title·description·robots를 바꾸지 않음). 비-ko 로케일 noindex |
| title | 중복 0, SERP 폭 55 이하, "종목명(티커) 주가 분석 — …" 형태 |
| 헤딩·랜드마크 | 페이지당 h1 정확히 1개, header·nav·main·footer 존재, 이미지 alt 누락 0 |
| 구조화 데이터 | JSON-LD 파싱 오류 0 |
| 내부 링크 | 색인 대상 차트 페이지 전부 다른 종목 페이지에서 1개 이상(중앙값 56) 링크됨 |
| 보안 헤더 | HSTS preload·nosniff·X-Frame-Options·frame-ancestors |

### 9-3. 발견과 조치

| # | 발견 | 규모 | 조치 |
|---|---|---|---|
| F1 | **UA별 본문 차이**: Googlebot 렌더의 `/market`·`/market/kr`에 "봇 트래픽으로 보여 분석 결과를 표시하지 않았어요…" 안내문. 사람 UA는 같은 시각에 브리핑 산문 약 400자 | 허브 2개 | 브리핑 액션 두 개에서 봇 분기 제거 (`fix/seo-live-audit`) |
| F2 | **하이드레이션 실패**: React #418이 모든 방문자·Googlebot에게. 서버(node:22-alpine, ICU 78.2)가 "PM 6:30"·"US$1952.0억", 브라우저가 "오후 6:30"·"US$1952억" | 뉴스·재무제표·경제·뉴스 허브 | node:24-alpine + ICU 빌드 가드 (#838) |
| F3 | **숫자만 바뀌는 탭**: `/{종목}/fear-greed` 공통 문장 92%, 고유 문자 중앙값 300자(차트 30%, 뉴스 26%) | 431 URL | 항상 noindex + sitemap 제외 (`fix/seo-live-audit`) |
| F4 | **sitemap에 noindex URL 254개**: 봉 없는 상장폐지 15종, ETF·펀드를 `stock`으로 분류해 실린 `/financials`, 산문 없는 `/congress` 108·`/overall` 49 | 254 URL | 15종 제거, ETF 목록 보강, congress·overall은 신선한 스냅샷 보유 종목만 (`fix/seo-live-audit`) |
| F5 | **얇은 meta description**: 평이화 첫 문장("애플 주가는 지금 332.41달러입니다.")만 담겨 70자 미만 | 차트 118·종합 104, 탭 간 중복 23쌍 | 평이화가 40자 미만이면 원문 필드로 (`fix/seo-live-audit`) |
| F6 | **사실과 반대인 빈 상태**: `/AAPL/overall`이 요약에서 풋콜비율을 인용하면서 옵션 섹션은 "옵션이 상장되어 있지 않거나…" | 옵션 bullet이 빈 종합 탭 | 빈 섹션은 렌더하지 않음 (`fix/seo-live-audit`) |
| F7 | **백테스팅 기간 표기**: 홈 카드·매니페스트 "2년", Dataset `temporalCoverage` 2024-04/2026-04 하드코딩, `dateModified`=배포 시각 | 전 페이지(매니페스트) | 기간 파생, 가짜 갱신일 제거 (`fix/seo-live-audit`) |

### 9-4. 남긴 것과 이유

| # | 발견 | 판단 |
|---|---|---|
| R1 | **AI 산문이 직접 매매 조언을 한다** — "지금 사기에는 위치가 불리합니다", "나눠서 사는 편이 낫습니다", "손절 기준은 …" (차트·종합 전 종목). `/about`의 "매수·매도를 권유하지 않습니다"와 정면으로 어긋나는 YMYL 신뢰 문제 | **siglens-core 프롬프트 작업**(SCOPE.md 가드). 여기서 고치지 않는다. 우선순위 1순위 후속 |
| R2 | 배포마다 ISR 캐시가 비워진다(`siglens-isr/{GIT_SHA}/`). 배포 직후 Googlebot이 받는 원시 HTML 88%가 CF·ISR 둘 다 MISS, 응답 p50 1.05초·p99 4.5초, 동시 5요청에서 p50 3.3초 | 크롤 용량 신호. 배포 후 sitemap 상위 URL 프리웜 또는 배포 빈도 조절을 별건으로 |
| R3 | 종목 탭 7종의 FAQ 3문항이 종목명만 바뀌는 템플릿(FAQ 리치 결과는 2023년부터 정부·보건 사이트 한정) | 공통 문장 비율을 올리는 주범 중 하나. 제거 여부는 다음 측정 후 |
| R4 | `/news/forex`가 빈 카테고리라 noindex인데 sitemap·전역 내비에 있다. `/economy*`·`/backtesting` lastmod가 여전히 배포 시각 | 낮음 |
| R5 | 원시 HTML에서 Suspense fallback과 본문이 `<main>`을 두 번 갖고, 차트 탭 h1이 sr-only로 먼저 온다(렌더 후엔 정상) | Google은 렌더 DOM을 본다. 비렌더 크롤러만 영향 |
| R6 | 관련 종목에 무관한 링 이웃(AAPL → LENZ·UNBX), 백테스팅 표의 12px 미만 텍스트 1,117개, 경제 지표 "159075천명" 같은 미포맷 숫자 | 낮음 |
| R7 | CWV: PSI 익명 할당량 소진으로 이번엔 미측정 | GSC › 코어 웹 바이탈에서 확인 |
