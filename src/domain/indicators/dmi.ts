import type { DMIResult } from '@/domain/types';

export function calculateDMI(
  highs: number[],
  lows: number[],
  closes: number[],
  period = 14,
): DMIResult[] {
  // TODO: implement
  void highs;
  void lows;
  void closes;
  void period;
  return [];
}
