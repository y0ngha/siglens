export type VWAPResult = {
  value: number;
  timestamp: string;
};

export function calculateVWAP(
  highs: number[],
  lows: number[],
  closes: number[],
  volumes: number[],
): VWAPResult[] {
  // TODO: implement
  void highs;
  void lows;
  void closes;
  void volumes;
  return [];
}