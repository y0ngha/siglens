export function calculateVWAP(
  highs: number[],
  lows: number[],
  closes: number[],
  volumes: number[],
): (number | null)[] {
  // TODO: implement
  void highs;
  void lows;
  void volumes;
  return closes.map(() => null);
}
