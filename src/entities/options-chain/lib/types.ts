/**
 * Options-domain types specific to the siglens application layer
 * (separate from raw chain/snapshot types exported by @y0ngha/siglens-core).
 *
 * Canonical definition lives in `shared/lib/types`; this module re-exports
 * for barrel convenience.
 */
export type { OptionsExpirationSelector } from '@/shared/lib/types';
