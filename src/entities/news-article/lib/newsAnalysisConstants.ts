/**
 * 동시 `runNewsCardAnalysis` 호출 상한 — `withConcurrencyLimit` 인자.
 *
 * `runNewsCardAnalysis`는 블로킹 LLM 왕복이다(worker 제거 이후). N개를
 * 무제한 병렬 실행하면 2-vCPU 서버에서 커넥션 풀 고갈 / 메모리 압박이 생긴다.
 * 카테고리 피드(50개, LLM_PARALLEL_LIMIT=8)보다 적은 경향이 있는 심볼별
 * 뉴스에서 4로 설정한다 — 처리량과 서버 여유 사이의 균형(경제 캘린더
 * CALENDAR_ANALYSIS_PARALLEL_LIMIT=4와 동일 근거).
 */
export const NEWS_CARD_ANALYSIS_PARALLEL_LIMIT = 4;

/**
 * 방문자 경로가 한 번에 보강할 기사 수 상한.
 *
 * 원래 상한이 없었다 — `waitUntil` 안이라 응답 마감이 없다는 이유였는데, 마감이
 * 없다는 것과 비용이 없다는 것은 다르다. 방문자 경로의 적재 lookback은 180일
 * (`NEWS_LOOKBACK_MS`)이고 FMP 상한이 1,000건이라, 백로그가 쌓인 종목의 첫 마운트
 * 한 번이 최악 1,000회 LLM 왕복이 된다. 동시 4개로 나눠도 2-vCPU 박스에서 LLM
 * 소켓 4개를 수십 분 잡는다(감사: 비용 확인 패스).
 *
 * 25는 집계 프롬프트 상한(`MAX_AGGREGATE_NEWS_ITEMS`)과 숫자를 맞춘 것이지,
 * "그보다 더 보강해도 읽히지 않는다"는 뜻이 **아니다**. 집계는 최신순이 아니라
 * 보강된 풀 전체를 `priceImpact`로 정렬해 상위 25건을 고르므로, 더 보강하면 그
 * 상위 25의 후보 풀이 넓어진다(프롬프트 크기는 그대로다). 상한 자체의 근거는
 * 품질이 아니라 **마운트 1회당 LLM 왕복 비용**이다.
 *
 * cron 상한(12)보다 큰 것은 방문자 경로엔 유닛 타임아웃이 없기 때문이다.
 *
 * 남은 기사는 다음 마운트나 다음 밤 cron이 이어받는다 — `analyzedAt`이 찍힌
 * 기사는 후보에서 빠지므로 진전은 누적된다.
 */
export const VISITOR_NEWS_CARD_LIMIT = 25;

/**
 * prewarm cron이 한 tick에 보강할 기사 수 상한.
 *
 * 방문자 경로는 `VISITOR_NEWS_CARD_LIMIT`(25)로 더 넉넉하다 — 유닛 타임아웃이
 * 없기 때문이다. cron만
 * `UNIT_TIMEOUT_MS`(2분) 안에서 카드 보강 + 집계 분석을 **모두** 끝내야 한다.
 *
 * 숫자 근거: 12건 / 동시 4 = 3청크. 프로덕션 실측 DeepSeek 왕복이 4~13초이므로
 * 최악 3×13 = 39초. 여기에 적재(2~5초)와 마지막 집계 LLM 호출이 더해져도 2분
 * 안에 들어온다. `MAX_AGGREGATE_NEWS_ITEMS`(25)를 그대로 쓰면 최악 7청크 ×13 =
 * 91초라 집계 호출 전에 예산을 거의 다 쓴다 — **백로그가 큰 종목일수록 타임아웃**
 * 이라, 하필 이 수정이 겨냥한 저트래픽 종목이 매일 밤 진전 없이 끝난다.
 *
 * 덜 보강해도 스냅샷은 나온다: 집계 분석은 기사가 몇 건이든 돌아가므로, 25건을
 * 채우려다 타임아웃 나는 것보다 12건으로 **그날 밤 안에 스냅샷을 만드는** 편이
 * 낫다. 남은 기사는 다음 거래일 경계에 이어받는다(같은 날 안에서는 harvest된 탭이
 * fresh로 잡혀 재시도되지 않는다).
 */
export const PREWARM_NEWS_CARD_LIMIT = 12;
