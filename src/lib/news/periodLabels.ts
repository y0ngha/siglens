/** Display label for the news list period (must match NEWS_LOOKBACK_MS = 180 days). */
export const NEWS_LIST_PERIOD_LABEL = '최근 6개월';

/** Display label for the AI aggregate analysis period (must match NEWS_ANALYSIS_LOOKBACK_MS = 28 days). */
export const NEWS_ANALYSIS_PERIOD_LABEL = '최근 28일';
