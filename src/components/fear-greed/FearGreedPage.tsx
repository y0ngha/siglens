interface FearGreedPageProps {
    symbol: string;
}

/** Temporary stub — Task 13 will replace with the full composition. */
export function FearGreedPage({ symbol }: FearGreedPageProps) {
    return (
        <main className="text-secondary-300 p-6 text-sm">
            공포·탐욕 지수 페이지 ({symbol}) — Task 13에서 구현 예정
        </main>
    );
}
